# debug_addi.by
# 测试ADDI指令的负数处理

.main
.code
LOADI     R0, 5            # R0 = 5
ADDI      R1, R0, -1       # R1 = R0 - 1 = 4
ADDI      R2, R0, -2       # R2 = R0 - 2 = 3
RETURN1   R1               # 返回R1 (应该是4)
.end

