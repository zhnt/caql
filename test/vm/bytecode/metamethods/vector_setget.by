# 向量直接索引读写应生效

.code
NEWOBJECT R0, 3, 2   # 创建向量，长度/容量=2
LOADI    R1, 0
LOADI    R2, 11
SETPROP  R0, R1, R2
LOADI    R3, 1
LOADI    R4, 22
SETPROP  R0, R3, R4
LOADI    R5, 1
GETPROP  R6, R0, R5
RETURN1  R6
.end
