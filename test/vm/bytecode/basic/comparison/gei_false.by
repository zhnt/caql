# GEI指令测试 - 立即数大于等于比较
# 测试: 3 >= 5 为假

.code
LOADI   R0, 3       # R0 = 3
GEI     R0, 5, 0    # 比较 R0 >= 5，如果为真则跳过JMP
JMP     2           # 跳转到失败分支
LOADI   R1, 1       # R1 = 1 (如果比较成功)
JMP     1           # 跳过失败分支
LOADI   R1, 0       # R1 = 0 (如果比较失败)
RETURN1 R1          # 返回 R1 (应该是 0)
.end
