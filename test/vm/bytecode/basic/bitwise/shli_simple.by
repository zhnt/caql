# 左移立即数操作测试
# 测试: 5 << 2 = 20 (使用立即数)

.code
LOADI   R0, 5       # R0 = 5 (二进制: 101)
LOADI   R1, 2       # R1 = 2 (移位数)
SHL     R2, R0, R1  # R2 = R0 << R1 = 20 (使用寄存器版本)
MMBIN   R2, R0, 15  # 元方法调用 (TM_SHL = 15)
RETURN1 R2          # 返回 R2 (应该是 20)
.end
