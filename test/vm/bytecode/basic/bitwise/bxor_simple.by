# 位异或操作测试
# 测试: 5 ^ 3 = 6

.code
LOADI   R0, 5       # R0 = 5 (二进制: 101)
LOADI   R1, 3       # R1 = 3 (二进制: 011)
BXOR    R2, R0, R1  # R2 = R0 ^ R1 = 6 (二进制: 110)
MMBIN   R2, R0, 14  # 元方法调用 (TM_BXOR = 14)
RETURN1 R2          # 返回 R2 (应该是 6)
.end
