# 位与立即数操作测试
# 测试: 15 & 7 = 7 (使用立即数)

.code
LOADI   R0, 15      # R0 = 15 (二进制: 1111)
LOADI   R1, 7       # R1 = 7  (二进制: 0111)
BAND    R2, R0, R1  # R2 = R0 & R1 = 7 (使用寄存器版本)
MMBIN   R2, R0, 12  # 元方法调用 (TM_BAND = 12)
RETURN1 R2          # 返回 R2 (应该是 7)
.end
