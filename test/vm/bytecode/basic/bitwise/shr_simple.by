# 右移操作测试
# 测试: 20 >> 2 = 5

.code
LOADI   R0, 20      # R0 = 20 (二进制: 10100)
LOADI   R1, 2       # R1 = 2 (移位数)
SHR     R2, R0, R1  # R2 = R0 >> R1 = 5 (二进制: 101)
MMBIN   R2, R0, 16  # 元方法调用 (TM_SHR = 16)
RETURN1 R2          # 返回 R2 (应该是 5)
.end
