# 位或操作测试
# 测试: 5 | 3 = 7

.code
LOADI   R0, 5       # R0 = 5 (二进制: 101)
LOADI   R1, 3       # R1 = 3 (二进制: 011)
BOR     R2, R0, R1  # R2 = R0 | R1 = 7 (二进制: 111)
MMBIN   R2, R0, 13  # 元方法调用 (TM_BOR = 13)
RETURN1 R2          # 返回 R2 (应该是 7)
.end
